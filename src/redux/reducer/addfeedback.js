import { normalizedExperience } from '../../fixtures';

export default (state = normalizedExperience, action) => {
     ;
    const { type } = action;
    switch (type) {
 
        default:
            return state ;
    }

}

